# NoMethodError: undefined method 'keys' for Array

# A. We're missing keys in an array variables

# B. There is no method called keys for Array objects

# C. keys is an array object, but it hasn't been defined yet

# D. There's an array of strings, and we're trying to get the string keys out of the array, but it doesn't exists

# answer B, the keys method is a hash method (returns an array of all keys of invoked hash), not an arrays method
